<snippet>
  <content><![CDATA[
  setup: >
    filter=${1:* #  if supplied, only return facts that match this shell-style (fnmatch) wildcard.}
    fact_path=${2:/etc/ansible/facts.d #  path used for local ansible facts (*.fact) - files in this dir will be run (if executable) and their results be added to ansible_local facts if a file is not executable it is read. File/results format can be json or ini-format}
  ]]></content>
  <tabTrigger>an-setup</tabTrigger>
  <scope>source.yaml</scope>
</snippet>
